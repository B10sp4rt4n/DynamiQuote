streamlit>=1.28.0
pandas>=2.0.0
matplotlib>=3.7.0
pyspellchecker>=0.7.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
openai>=1.0.0
jinja2>=3.1.0

# PDF generation with ReportLab (Python pure, no system dependencies)
reportlab>=4.0.0

# API-First Architecture Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
requests>=2.31.0

# Testing Dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0

## Additional dependencies required for running on Streamlit Cloud
# - Pillow: reemplazo de PIL usado para manipular imágenes
# - openpyxl: motor recomendado para que pandas lea archivos Excel (.xlsx)
# Note: WeasyPrint needs system libraries (cairo, pango, gdk-pixbuf). Streamlit Community Cloud
# does NOT provide those system packages; see README or use a Docker deploy for full WeasyPrint support.
Pillow>=9.0.0
openpyxl>=3.1.0
